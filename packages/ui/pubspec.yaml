name: ui
description: Shared UI components and design system
version: 0.1.0

publish_to: none

environment:
  sdk: ">=3.10.0 <4.0.0"
  flutter: ">=3.10.0"

resolution: workspace

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.1
